DATA    SEGMENT
BUFFER  DB  34H,-3BH,-5AH,72H,0H,-2DH,0H,66H
RESULT  DB  3  DUP(3)
DATA	ENDS
CODE    SEGMENT
	ASSUME CS:CODE,DS:DATA
START:  MOV AX,DATA
	MOV DS,AX
	MOV CX,8H
	XOR BX,BX
	XOR AX,AX
	MOV SI,OFFSET BUFFER
NEXT:   MOV AL,[SI]
	SHL AL,1
	JC  MINUS
	AND AL,AL
	JZ  ZERO
	INC BH
	JMP EOL
ZERO:   INC BL
	JMP EOL
MINUS:  INC DH
EOL:    INC SI
	LOOP NEXT
	XOR AX,AX
	MOV AL,DH
	ADD AL,30H
	MOV DL,AL
	XOR DH,DH
	MOV AH,2H
	INT 21H
	MOV AL,BL
	ADD AL,30H
	MOV DL,AL
	MOV AH,2H
	INT 21H
	MOV AL,BH
	ADD AL,30H
	MOV DL,AL
	MOV AH,2H
	INT 21H
	MOV AX,4C00H
	INT 21H
CODE	ENDS
	END START
	
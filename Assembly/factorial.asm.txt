DATA SEGMENT
NN DB 9H
DATA ENDS

CODE SEGMENT
ASSUME 	CS:CODE,DS:DATA
START: 	MOV AX,DATA
	MOV DS, AX
	MOV BL, NN
	CALL FACT
	MOV AX, 4C00H
	INT 21H

FACT	PROC NEAR
	MOV AX, 1
MUL24:	PUSH DX
	MUL BX
	MOV CX,AX
	POP AX
	MUL BL
	ADD AX,DX
	MOV DX,AX
EMUL24:	MOV AX,CX
	CMP BX, 3
  	JS  RETURN
  	DEC BX
  	JMP MUL24
RETURN:	RET
FACT ENDP
  
CODE ENDS

END START

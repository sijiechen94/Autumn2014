DATA SEGMENT
DIS DW 87FAH
DATA ENDS

CODE SEGMENT
ASSUME 	CS:CODE,DS:DATA
START: 	MOV AX,DATA
	MOV DS, AX
	MOV AX, DIS
	CALL PAXB
	MOV AX, 4C00H
	INT 21H

PAXB	PROC NEAR
  	MOV BX, AX
  	MOV AX, 200H
  	MOV CX, 10H
BITS:	SHL BX, 1
	JNC ZERO
ONE:	MOV DL,31H
	JMP PRINT
ZERO:	MOV DL,30H
PRINT:	INT 21H
  	LOOP BITS
  	RET	
PAXB ENDP
  
CODE ENDS
END START

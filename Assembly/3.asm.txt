DATA	SEGMENT
BUFFER  DB  34H,67H,0E2H,0A7H,89H,73H,51H,43H,0E2H,0A7H,89H,73H,67H,51H,43H,34H
TEMP    DW  ?
DATA    ENDS
CODE	SEGMENT
	ASSUME CS:CODE,DS:DATA
START:  MOV AX,DATA
	MOV DS,AX
	XOR AX,AX
	MOV CX,10H
	MOV SI,OFFSET BUFFER
OUTER:	XOR BX,BX               ;BX stores max
	MOV TEMP,CX
  	MOV AX,10H
	SUB AX,TEMP
	MOV SI,AX
	XOR AX,AX
INNER:	MOV AL,[SI]
	CMP AL,BL
	JC  SMALLER
BIGGER: MOV BL,[SI]
	MOV DI,SI
SMALLER:INC SI
	LOOP INNER
POPUP:  MOV AX,10H
	SUB AX,TEMP
	MOV SI,AX
	MOV AL,[SI]
	MOV [DI],AL
	MOV [SI],BL
	MOV CX,TEMP
	LOOP OUTER
	MOV AX,4C00H
	INT 21
ENDS	CODE
	END START